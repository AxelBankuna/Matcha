function update_distance(val) {
    document.getElementById('show_distance').value=val;
}

function update_age(val) {
    document.getElementById('show_age').value=val;
}

function update_frating(val) {
    document.getElementById('show_frating').value=val;
}